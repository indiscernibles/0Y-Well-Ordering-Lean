# 0-Y 与 BM4 的等价及 0-Y 良序性

初稿：2026-09-09；最终形式化验证：2026-09-10。对应 Lean 4 工程及逐项验证状态见 [形式化说明](formalization/README.md)。

本文证明全部合法 0-Y 表达式与全部可逆矩阵之间的序同构，并证明标准表达式集及任意固定合法起点的展开闭包按字典序良序。先采用独立定义的“完整坏部”数学基本列，再证明它与 Wiki 的截断约定在任意起点生成相同可达闭包。证明以本文及 Lean 工程中明确给出的数学算法为准，不假定所给 HTML 互译程序准确。

## 一、命题与约定

有限序列从 0 编号；不存在的父项记作 $\bot$。矩阵按列书写，列从第 0 行向上编号。列末补零不改变矩阵；因而可以把每列视为最终恒零的自然数序列。矩阵仍保留列数，例如一个零列、两个零列和没有列是三个不同对象。

令 $\mathcal L_Y$ 为所有首项是 1 的有限正整数序列，加上空序列。令 $E_n$ 为下文独立定义的完整坏部山脉展开，$n\in\mathbb N$ 包含 0。令 $B_n$ 为 BM4 展开。两侧空表达式都展开为空表达式。

记 $\mathcal S_Y$ 为种子 $(1,2),(1,3),\ldots$ 在所有 $E_n$ 下的闭包，$\mathcal S_B$ 为 $(0^r)(1^r)$，$r\ge1$，在所有 $B_n$ 下的闭包。字典序规定真前缀较小；矩阵先比较列，再按列内字典序比较。

**定理。** 下文定义的深度编码 $D$ 与求和解码 $F$ 满足：

1. 对每个 $s\in\mathcal L_Y$，$D(s)$ 有限，且 $F(D(s))=s$。
2. 对所有合法 $s,t$，$s<_{\rm lex}t\iff D(s)<_{\rm lex}D(t)$。
3. 对所有合法 $s$ 和 $n\in\mathbb N$，
   $$D(E_n(s))=B_n(D(s)).$$
4. 令 $\mathcal R_B=\{A:D(F(A))=A\}$，矩阵按共同尾零行等同，则 $D:\mathcal L_Y\to\mathcal R_B$ 是序同构，逆映射为 $F|_{\mathcal R_B}$。这不要求表达式标准。
5. 限制到标准式，$D:\mathcal S_Y\to\mathcal S_B$ 仍是序同构，并逐项保持上述基本列。
6. Wiki 的 0-Y 基本列生成的标准式集合同样是 $\mathcal S_Y$。因此通常的标准 0-Y 在字典序下良序，且与标准 BM4 有相同序型。
7. 论文的稳定表示方法还能推出：任意有限矩阵的非平凡展开关系良基；每个固定起点的展开闭包按字典序良序。经转换，任意合法 0-Y 起点也具有这两个性质。这是第七节补出的加强推论，不表示全体合法表达式合起来按字典序良序。

这里的良序结论不能扩大到全部合法表达式：
$$ (1,2)>_{\rm lex}(1,1,2)>_{\rm lex}(1,1,1,2)>_{\rm lex}\cdots . $$

## 二、共同的父图表示

### 2.1 差分山脉与深度编码

给定 $s=(s_0,\ldots,s_{m-1})\in\mathcal L_Y$，令 $x_0(i)=s_i$。令 $P_{-1}(i)=i-1$，其中 $P_{-1}(0)=\bot$。逐层定义

$$P_r(i)=\text{沿 }P_{r-1}\text{ 严格祖先链遇到的第一个满足 }x_r(j)<x_r(i)\text{ 的 }j,$$

找不到时取 $\bot$，并令

$$x_{r+1}(i)=\begin{cases}x_r(i)-x_r(P_r(i)),&P_r(i)\ne\bot,\\1,&P_r(i)=\bot.\end{cases}$$

在同一行从左到右定义深度
$$h_r(i)=\begin{cases}h_r(P_r(i))+1,&P_r(i)\ne\bot,\\0,&P_r(i)=\bot.\end{cases}$$

$D(s)$ 的第 $i$ 列是 $(h_0(i),h_1(i),\ldots)$。空序列映为空矩阵。

**引理 1（有限性）。** 每层都是正整数，且 $P_r(i)=\bot$ 当且仅当 $x_r(i)=1$。山脉最终全为 1，故 $D(s)$ 只有有限个非零行。

**证明。** 第 0 层的大于 1 的项至少能找到最左边的 1。归纳地，上一层每棵父树的根在本层仍取值 1。如果本层某项大于 1，它不可能是上一层的根，而沿上一层祖先链最终可到达一个值为 1 的根，故本层有父项。值为 1 的项没有更小的正整数父项。

设 $M_r=\max_i x_r(i)$。当 $M_r>1$ 时，
$$M_{r+1}\le\max(1,M_r-1).$$
至多 $\max(s)-1$ 次差分后全为 1，此后所有深度为 0。$\square$

### 2.2 深度替换不改变父图

**引理 2（栈引理）。** 给定一个每条边指向左方的森林 $Q$，节点有全序标签。沿 $Q$ 祖先链取最近的严格较小标签，得到父图 $P$。把标签替换为各节点的 $P$ 深度，再沿同一个 $Q$ 链取最近较小项，仍得到 $P$。

**证明。** 固定一条从 $Q$ 根至某节点的路径。路径上各节点的所有 $P$ 祖先也在此路径中，故可当作线性序列处理。最近较小项由单调栈计算：弹出标签不小于新标签的栈顶，剩余栈顶为父，压入新节点。

栈中的节点恰为当前节点的父链，其深度依次为 $0,1,\ldots$。若新节点的父为深度 $d-1$ 的节点，原算法弹出的栈后缀全部具有深度至少 $d$；换用深度标签会恰好弹出同一后缀。无父时新深度为 0，整栈被弹出。扫描归纳后，在每条路径上均得到相同父图。$\square$

BM4 的第 0 行父项是在左侧取最近较小项，后续行是在前一行父链上取最近较小项。对行数归纳应用引理 2，得到：

**推论 3。** $D(s)$ 按 BM4 定义计算的父图恰为 $P_r$，矩阵的每个元素就是该行父链的深度。

### 2.3 求和解码

给有限矩阵 $A$ 计算 BM4 父图 $p_r$。从所有有效行上方的一行 1 开始，向下逐行、每行从左向右作
$$v_r(i)=\begin{cases}v_{r+1}(i)+v_r(p_r(i)),&p_r(i)\ne\bot,\\v_{r+1}(i),&p_r(i)=\bot.\end{cases}$$
定义 $F(A)=(v_0(0),\ldots,v_0(m-1))$。

**推论 4。** 对每个合法 $s$，$F(D(s))=s$。

**证明。** 父图由推论 3 相同；引理 1 保证无父项的值为 1；有父项时，差分递推恰为求和递推的逆式。先按行自上而下，再按列自左向右归纳即得 $v_r=x_r$。$\square$

不能将此推论倒置为任意矩阵的往返定理。例如 $(0,0)(1,0)(1,1)$ 经 $F$ 得到 $(1,2,3)$，再经 $D$ 得到 $(0)(1)(2)$。以下专门证明正确的矩阵类别在 BM4 展开下封闭。

## 三、比较引理与矩阵的精确合法性条件

### 3.1 共用候选链的比较

**引理 5（共链比较）。** 固定一条候选祖先链及链上已经确定的山脉。为新节点赋不同的正整数值。对这条候选链作逐层最近较小项、差分和深度编码，则值的严格大小与所得深度列的字典序严格大小相同。相同深度列对应相同值。

这里候选链为空时只允许根值 1；不为空时，其根值为 1。

**证明。** 随着阈值 $a$ 增加，可能成为“最近且小于 $a$”的候选，依次是该链的后缀严格最小值。这些候选组成一条父链，它们的深度严格递增。因此新节点深度不减；若深度相同，所选父项必相同。

对 $a<b$，若本行深度不同，方向已经确定；否则同时减去同一父项的值，保持严格不等，并得到下一行的同一候选链。此过程不能永远深度相同：较小值最终降至 1，深度为 0；较大值仍大于 1，有父项，深度为正。$\square$

所有计算仅依赖当前列及左侧，故 $D$ 保持前缀。对两个序列首个不同位置应用引理 5，得到定理第 2 项。

### 3.2 深度正规矩阵与阻挡条件

称矩阵 $A=(a_r(i))$ **深度正规**，若它的 BM4 父图 $p_r$ 满足
$$a_r(i)=0\quad(p_r(i)=\bot),\qquad a_r(i)=a_r(p_r(i))+1\quad(p_r(i)\ne\bot).$$

以 $p_{-1}(i)=i-1$ 统一处理第 0 行。记列后缀
$$T_r(i)=(a_r(i),a_{r+1}(i),\ldots).$$

对每个 $r,i$，令 $q=p_{r-1}(i)$，$p=p_r(i)$。若 $p\ne\bot$ 且 $p\ne q$，则 $p$ 在 $q$ 的 $p_r$ 父链上。令 $z$ 为这条链中紧接在 $p$ 下方的节点，即
$$p_r(z)=p,\qquad z\text{ 是 }q\text{ 的非严格 }p_r\text{ 祖先。}$$
称 $z$ 为此处的**阻挡节点**。要求
$$\tag{S} T_{r+1}(i)\le_{\rm lex}T_{r+1}(z).$$
当 $p=\bot$ 或 $p=q$ 时，此处不添加条件。

上述 $p$ 必在 $q$ 父链上的事实，就是对矩阵第 $r$ 行标签沿第 $r-1$ 行路径运行单调栈：新父项属于加入新节点前的栈。

**引理 6（识别引理）。** 深度正规矩阵 $A$ 属于 $D(\mathcal L_Y)$，当且仅当它满足所有条件 (S)。在此情况下 $D(F(A))=A$。

**证明。** 深度正规和父链逐层嵌套保证：若 $p_r(i)=\bot$，该列从第 $r$ 行起全部为 0，所以求和解码给出的 $v_r(i)=1$。

对 $r$ 从最高行向下归纳。精确的归纳断言是：$p_k$ 等于对数值 $v_k$ 沿**给定森林** $p_{k-1}$ 作最近较小项搜索的结果。这个断言不要求 $p_{k-1}$ 已经从更低层重算验证，故向下归纳没有循环依赖。在第 $r$ 行，再按列从左向右归纳。因此，加入第 $i$ 列以前，第 $r$ 行的 $p_r$ 父链就是数值标签 $v_r$ 的单调栈。

无父时 $v_r(i)=1$，所有候选均不小于它，选父正确。有父时
$$v_r(i)=v_r(p)+v_{r+1}(i)>v_r(p).$$
若 $p=q$，它已经是最近候选，无须弹栈。否则，要恰好弹到 $p$，充要条件是 $v_r(i)\le v_r(z)$：栈上从 $z$ 至 $q$ 的数值严格递增，故此一个比较就保证整个所需后缀被弹出。

由于 $i,z$ 有共同的 $p_r$ 父项 $p$，该比较等价于
$$v_{r+1}(i)\le v_{r+1}(z).$$
更高各行已规范，且两节点在第 $r+1$ 行共用同一候选链，故由引理 5，这又等价于 (S)。这样便完成双重归纳，证明 (S) 足以保证重新计算父项时恢复全部 $p_r$。原矩阵元素已经等于这些父链的深度，故 $D(F(A))=A$。

反过来，若 $A=D(s)$，规范山脉在相同单调栈中确实选中 $p$，于是必有上面同样的阻挡不等式，再由引理 5 得到 (S)。$\square$

**推论 6.1（任意矩阵的可逆充要条件）。** 对任意有限非负整数矩阵 $A$，按共同尾零行等同，有
$$D(F(A))=A\quad\Longleftrightarrow\quad A\text{ 深度正规且满足全部条件 (S)}.$$
充分性是引理 6。必要性是：等式使 $A$ 成为某个合法序列 $F(A)$ 的深度编码，故必深度正规；然后由引理 6 得到 (S)。此判据只使用矩阵父项和列后缀的比较，不必先执行往返转换；它判定的是可逆性，不是标准式生成性。

例如 $A=(0,0)(1,0)(1,1)$ 满足深度正规，但在 $r=0,i=2$ 处，$q=1,p=0,z=1$。条件 (S) 要求
$$T_1(2)=(1,0,\ldots)\le_{\rm lex}(0,0,\ldots)=T_1(1),$$
显然不成立。实际求和得到 $(1,2,3)$，其最后一个数的父项变为第 1 列，而原矩阵第 0 行最后一列的父项是第 0 列，所以再编码变成 $(0)(1)(2)$。

### 3.3 全部可逆表达式的序同构

定义 $\mathcal R_B=\{A:D(F(A))=A\}$。根据推论 6.1，这也就是全部满足深度正规性与条件 (S) 的矩阵，无须附加标准性。

**定理 6.2。** 对合法序列使用通常字典序，对矩阵使用列补尾零后的列内字典序、再按列作字典序，有
$$D:(\mathcal L_Y,<_{\rm lex})\;\cong\;(\mathcal R_B,<_{\rm lex}),\qquad D^{-1}=F|_{\mathcal R_B}.$$

**证明。** 首先，对任意有限非负矩阵 $A$，求和递推从 1 出发，只作保留或正整数相加，因此 $F(A)$ 的各项为正整数；第一列没有父项，在各层始终为 1。空矩阵输出空序列。所以 $F(A)\in\mathcal L_Y$。

其次，对任意 $s\in\mathcal L_Y$，推论 4 给出 $F(D(s))=s$，故
$$D(F(D(s)))=D(s).$$
于是 $D(s)\in\mathcal R_B$。反过来，任意 $A\in\mathcal R_B$ 都满足 $A=D(F(A))$，因此是某个合法序列的编码。这证明
$$\mathcal R_B=D(\mathcal L_Y).$$
而 $F(D(s))=s$ 还保证 $D$ 单射，故两侧互转构成双射。

最后，保序论证完全不依赖标准性。若 $s$ 是 $t$ 的真前缀，所有层的前缀计算相同，故 $D(s)$ 是 $D(t)$ 的真前缀。否则，设 $i$ 是首个不同位置且 $s_i<t_i$。更左各列的山脉和父图相同，第 0 行候选链也相同。应用引理 5，在第一行深度不同的位置，$s$ 的深度较小；若某层深度相同，则所选父项相同，同时减去该父项值，仍保持数值严格不等和下一层候选链相同。较小值最终降至 1 时，深度为 0，较大值的深度为正，故必在有限行内出现正确方向的首次差异。因此
$$s<_{\rm lex}t\Longrightarrow D(s)<_{\rm lex}D(t).$$
由于两侧字典序均为全序，且 $D$ 是单射，反方向也成立。对 $s=F(A),t=F(B)$ 应用此结论，得到
$$A<_{\rm lex}B\iff F(A)<_{\rm lex}F(B)\qquad(A,B\in\mathcal R_B).$$
因此 $D$ 是全域序同构。$\square$

**这个全域结论不是良序结论。** 对每个 $k\ge1$，令 $s_k=(\underbrace{1,\ldots,1}_{k\text{ 个}},2)$，则
$$D(s_k)=\underbrace{(0)\cdots(0)}_{k\text{ 列}}(1).$$
这些矩阵全部满足可逆充要条件，但
$$s_1>s_2>s_3>\cdots,\qquad D(s_1)>D(s_2)>D(s_3)>\cdots.$$
所以同构的是两个非良序的线性序集；良序性在后文的标准子集上成立。

这里的“非良序”只针对全域字典序，不表示存在无限合法展开。第 7.1 节另证任意起点的展开终止性，以及每个固定起点的展开闭包的字典序良序性。

还须区分全域字典序与展开生成关系。后文的全域展开交换式表明，$D$ 也保持并反映每一步展开，从而保持并反映有限展开可达关系。但在全体表达式上，这个可达关系不等于字典序。例如 $(1,1,2)<_{\rm lex}(1,2)$，而两者任何一步展开都只剩若干个 1，此后只能删尾，因此两者互不可达。标准子集上的全序桥接不能直接用于整个 $\mathcal L_Y$。

## 四、BM4 展开保持条件 (S)

这是证明的核心。下面把边界处的父项逐一列出，避免把“复制图”直接当作“重新计算得到的图”。

设 $A$ 深度正规且满足 (S)，最后列下标为 $c$。若末列无父项，BM4 只删尾，所有性质立即由前缀性保持。以下设
$$d=\max\{r:p_r(c)\ne\bot\},\qquad b=p_d(c),\qquad L=c-b.$$
第 $t$ 块中原列 $j\in[b,c)$ 的复制记为 $j_t=j+tL$，$0\le t\le n$。$t=0$ 表示原坏部。令
$$\Delta_r=a_r(c)-a_r(b)>0\quad(r<d).$$
记 $b\preceq_r j$ 表示 $b=j$ 或 $b$ 是 $j$ 的第 $r$ 行祖先。BM4 的定义给出
$$\tag{1} a'_r(j_t)=a_r(j)+t\Delta_r\,\mathbf1_{b\preceq_r j}\quad(r<d),$$
而 $r\ge d$ 时 $a'_r(j_t)=a_r(j)$；好部不变。

### 4.1 展开后的父图

使用 Vargovčík 论文引理 2.5 的祖先复制结论，可以得到以下父项形式：

* 好部的父项不变。
* 非首列 $j_t$，$b<j<c$：原父项在好部则固定，在坏部则取同块复制，无父项则仍无父项。
* 新块首列 $b_t$，$t\ge1$：当 $r<d$，
  $$\tag{2}p'_r(b_t)=p_r(c)_{t-1};$$
  当 $r\ge d$，$p'_r(b_t)=p_r(b)$，该父项在好部或不存在。

说明其推导：论文引理 2.5(i)(ii)(iv) 给出非首列祖先在好部/同块中的精确复制，取最近祖先即可得到非首列公式。(iii) 给出 $r<d$ 时上一块内的祖先恰为原末列的对应祖先，且 $p_r(c)\ge b$，故得到 (2)。

在 $r=d$，新首列取值 $a_d(b)$；沿前一行祖先链在各坏部遇到的节点，该行值至少为 $a_d(b)$，因而不能作为严格较小父项。具体地，在原末列前一行祖先链中，$b$ 至末列之间的候选值都至少为 $a_d(c)=a_d(b)+1$；复制后的链按顺序重复原链从 $p_{d-1}(c)$ 至 $b$ 的闭段，再接上原 $b$ 的前一行祖先链。每段经过值为 $a_d(b)$ 的块首列，最终才进入好部。因此最近较小父项恰为原 $p_d(b)$。$d=0$ 时，这里的前一行链就是已定义的相邻链 $p_{-1}$。在 $r>d$，前一行的父项已经固定在好部，候选链和值均与原列 $b$ 相同，逐行归纳即可。

这些公式还证明 $A'$ 仍深度正规。非首列与其同块父项提升相同，或二者都不提升，故父子差仍为 1。新首列在 $r<d$ 时满足
$$a'_r(b_t)=a_r(b)+t\Delta_r=a_r(c)+(t-1)\Delta_r
=a'_r(p_r(c)_{t-1})+1.$$
其余行由原深度正规性直接得到。

### 4.2 同块后缀比较保持

**引理 7。** 设原节点 $u,v\in(b,c]$ 在第 $r$ 行有相同父项 $p\ge b$。对它们的后缀都实施式 (1) 的同一个第 $t$ 块变换，则原后缀字典序的相等、严格小于关系均保持。允许 $u=c$ 或 $v=c$；这时只是暂时保留原末列用于比较。

**证明。** 在第 $r+1$ 行，二者共用同一个候选祖先链。只要前面的后缀深度相同，所选父项也相同：沿候选链的后缀最小值组成父链，而深度在该链上严格递增。所以逐行仍共用候选链，相同深度也给出相同的 $b$ 祖先指示。

在首个不同的行 $k$，设 $a_k(u)<a_k(v)$。在共同候选链上，较大的新节点深度对应同一个或更靠近新节点的父项。因此，若 $b$ 是 $u$ 的第 $k$ 行祖先，它也是 $v$ 的第 $k$ 行祖先。也就是说，两者的提升指示满足 $\epsilon_u\le\epsilon_v$。$\Delta_k\ge0$，故
$$a_k(u)+t\Delta_k\epsilon_u<a_k(v)+t\Delta_k\epsilon_v.$$
$k\ge d$ 时该行不提升，更直接成立。若所有后缀行原来都相同，各行提升也相同。$\square$

### 4.3 验证所有阻挡条件

固定展开后的一个条件 (S)。好部条件不变。其余分为非首列和新首列。

**情形 A：非首列 $i=j_t$，$b<j<c$。** 记原父项 $p=p_r(j)$。没有父项或父项等于前一行父项时没有条件需要检查。

若 $p\ge b$，原阻挡节点 $z$ 也在坏部，展开后阻挡节点是 $z_t$。两列在同块、共同父项是 $p_t$，所以引理 7 将原条件直接传递到新条件。

若 $p<b$，$b$ 不是 $j$ 的第 $r$ 行祖先，故也不是其任何更高行的祖先；$j_t$ 的整个 $T_{r+1}$ 后缀不提升。对原阻挡节点 $z$ 有三种可能：

1. $z<b$：新阻挡节点仍为 $z$，后缀不变。
2. $b<z<c$：新阻挡节点为 $z_t$；它也以好部的 $p$ 为第 $r$ 行父项，因此后缀同样不提升。
3. $z=b$：若 $r<d$，复制首列的第 $r$ 行父链经由前面的块首列最终到达 $b_0$，所以 $p$ 下方的阻挡节点是 $b_0$；若 $r\ge d$，阻挡节点是 $b_t$。前一种用原 $b$ 的后缀，后一种所有这些行均不提升，也用原 $b$ 的后缀。

三者都把新条件还原为原条件。这里第三种情形正是需要注意的跨块边界。

**情形 B：新首列 $i=b_t$，$t\ge1$。**

若 $r<d$，它的前一行父项和本行父项分别来自上一块中原末列 $c$ 的两个父项。原本行父项 $p_r(c)\ge b$。若需阻挡节点，它就是原末列阻挡节点 $z$ 在上一块的复制 $z_{t-1}$。

暂将原末列也按第 $t-1$ 块公式变换，记其比较列为 $C^*$。新首列与 $C^*$ 在所有 $k<d$ 行相等，因为
$$a_k(b)+t\Delta_k=a_k(c)+(t-1)\Delta_k.$$
在第 $d$ 行，深度正规性给出
$$a'_d(b_t)=a_d(b)=a_d(c)-1=C^*_d-1.$$
故 $T'_{r+1}(b_t)<_{\rm lex}T_{r+1}(C^*)$。另一方面，原末列与 $z$ 的条件由引理 7 传递为
$$T_{r+1}(C^*)\le_{\rm lex}T'_{r+1}(z_{t-1}).$$
合起来即为所需新条件。

若 $r=d$，无父时无需条件。有父时，该父项为好部中的 $p_d(b)$；阻挡节点是上一块首列 $b_{t-1}$。两首列从第 $d+1$ 行起的后缀完全相等，故条件成立。

若 $r>d$，新首列的前一行候选链全部在好部，它的后缀和父项均与原列 $b$ 相同，所以条件与原条件相同。

所有条件均已覆盖。因此：

**推论 8。** 深度正规并满足 (S) 的矩阵类别在 BM4 展开下封闭。由引理 6，它正是 $D(\mathcal L_Y)$。

## 五、展开交换与标准式双射

令 $A=D(s)$，$A'=B_n(A)$。推论 8 给出 $D(F(A'))=A'$。

若 $s$ 为空或末项为 1，两侧都作相同的空操作或删尾。否则，由引理 1 和推论 3，BM4 的最高活动行 $d$ 正是 0-Y 中末列的父差首次等于 1 时停止的行，也是末列仍有父项的最高行；两者坏根同为 $b$。在该行
$$x_d(c)-x_d(b)=1.$$

在 $r\ge d$ 的各行，展开后的父图都只作好部固定、同块复制；由上而下的求和归纳可得，$v'_d$ 恰是原第 $d$ 行坏部的完整重复。在 $r<d$ 的各行，4.1 的父图公式恰为 0-Y 的复制规则：内部父项同块平移，位于好部的父项固定，新块首列继承上一块对应末列的父项。向下求和唯一确定结果，故
$$F(A')=E_n(s).$$
于是
$$D(E_n(s))=D(F(A'))=A'=B_n(D(s)),$$
证明了全体合法序列上的展开交换。

种子满足
$$D(1,r+1)=(0^r)(1^r).$$
由有限展开次数归纳，$D(\mathcal S_Y)\subseteq\mathcal S_B$。反过来，任取从某个 BM4 种子出发的有限展开过程，把同一串下标施加到对应 0-Y 种子，展开交换保证得到对应矩阵，故 $\mathcal S_B\subseteq D(\mathcal S_Y)$。

结合推论 4 的单射性及引理 5 的保序性，得到两侧标准式的双射、双向往返和序同构，且基本列逐项对应。并未把“有一个互转程序”作为论据。

## 六、与 Wiki 基本列约定的关系

Wiki 的基本列 $W_q$（$q\ge1$，按“复制 $q-1$ 次”的规则编号）保留的最后位置与完整坏部约定不同。两者来自同一个无限复制山脉，具体理由如下。Wiki 在最高活动行将原末项减 1，得到的值恰为 $x_d(b)$，所以它复制的区间 $(b,c]$ 与本文复制的区间 $[b,c)$ 只是同一个周期的不同切口。在较低各行，周期边界沿用原末列父项，非边界沿用原相应列父项；父项严格在 $b$ 左方时固定，其余端点相应平移。这正是 4.1 的低行父项公式。因此最高行的共同位置数值相同，向下按行、向右按列作求和归纳，所有共同位置的数值也相同。

若原长为 $m=c+1$、坏部长为 $L$，则
$$|E_n(s)|=c+nL,\qquad |W_q(s)|=c+1+(q-1)L.$$
因此
$$W_q(s)\text{ 是 }E_q(s)\text{ 的前缀},\qquad E_n(s)\text{ 是 }W_{n+1}(s)\text{ 的前缀}.$$
一般不能声称这两个版本的同下标基本列逐项相等。

完整坏部版本以 $E_0$ 实现删尾，因此其标准闭包对前缀封闭。Wiki 版本也如此：$W_1$ 恰将末项减 1。证明是在坏根行将原末项减 1，然后沿原末列的固定父项向下求和，每层末项都恰减 1。反复使用 $W_1$，末项达到 1 后取前驱，就实现了删尾；不需要预先假定良序。

所以两种闭包都对有限前缀封闭。由上述互为前缀关系，从任意固定合法起点作路径归纳，两种展开的可达闭包相等；再对共同种子取并，得到两个标准式集合相等。完整坏部版本和 Wiki 版本的基本列有限截断位置不同。

例如 Wiki 有
$$W_3(1,4,6,4)=(1,4,6,3,7,10,6,11,15,10),$$
而完整坏部版本还有末尾的 $16,21$。这不影响标准式集合和序型。

## 七、良序性

Vargovčík 的 BM4 良序定理适用于种子展开闭包，特别给出“非平凡展开的有限传递闭包”是严格良序。本文把共同尾零行视为同一表示，与论文及 Lean 的删尾零行规范数组逐一对应，并且展开相容。论文还允许零行种子；这个种子只是两个零列，已经由一行种子展开一次得到，所以不会增加本文的生成集合。

为免把不同有限列长度的字典序与本文“补无限尾零”的字典序混用，另作如下桥接。每次非空 BM4 展开在本文字典序下严格下降：$n=0$ 或末列无父项时取真前缀；其余情形在第一新列 $b_1$ 与原末列 $c$ 首次比较，所有 $r<d$ 行相等，第 $d$ 行则前者严格较小。故展开生成关系包含于本文字典序。前者已经是全序，所以反向包含也成立：若 $A<_{\rm lex}B$，全序性排除相等；若反向生成下降，则又有 $B<_{\rm lex}A$，矛盾。因此本文字典序同样良序。

设 $X\subseteq\mathcal S_Y$ 非空。则 $D(X)\subseteq\mathcal S_B$ 非空，因 BM4 良序有最小元 $D(s_0)$。对任意 $s\in X$，序嵌入性质给出
$$D(s_0)\le D(s)\quad\Longrightarrow\quad s_0\le s.$$
因此 $s_0$ 是 $X$ 的最小元，$\mathcal S_Y$ 良序。由第六节，这就是 Wiki 标准 0-Y 的良序性。$\square$

等价地，任何无限严格下降的标准 0-Y 链经 $D$ 都会给出无限严格下降的 BM4 链，与已有定理矛盾。

### 7.1 不限标准式的加强：任意起点的展开良基性

**范围澄清。** 用户提供的 v2 与公开 v3 都在定义 1.1 中把 BMS 定义为标准种子的展开闭包，定理 2.7 字面上证明这个集合良序。但祖先复制引理 2.5 针对任意非空矩阵，定理 2.7 中“已有稳定表示，则展开后可降低表示上界”的局部论证也不要求标准性。补上下面的初始表示构造，即可推广到所有有限矩阵。

**定理 9。** 在全部有限非负整数矩阵上，非平凡 BM4 展开关系及其非空有限传递闭包都是良基关系。特别地，无论各步怎样选择自然数参数，从任意有限矩阵出发，连续非空展开都会终止。

**证明。** 先证明任意矩阵都有论文方法所需的稳定表示。给定一个 $h$ 行、$m$ 列矩阵 $A$，$h\ge1,m\ge2$，考虑
$$U_{h,m}=(0^h)(1^h)\cdots((m-1)^h).$$
它是标准种子 $(0^{h+1})(1^{h+1})$ 在参数 $m-1$ 处的一次展开，删除共同尾零行后的结果。因此，论文对标准生成项的构造已经给出了 $U_{h,m}$ 的有界稳定表示 $f$。

在 $U_{h,m}$ 的每个有效行中，每个较早列都是每个较晚列的祖先。所以 $f$ 满足
$$i<j\Longrightarrow f(i)<f(j),\qquad
i<j,\ r<h\Longrightarrow f(i)<_r f(j).$$
而 $A$ 的稳定表示只要求其中一部分祖先关系成立。故把相同的标签 $f(i)$ 放在 $A$ 的第 $i$ 列，就得到 $A$ 的有界稳定表示。零行矩阵可以借用 $U_{1,m}$ 的表示；零列、一列的退化情况没有祖先约束，可直接限制某个标准矩阵的表示。

这里使用与论文反射构造相同的可用标签范围。若显式采用论文中的 $\sigma$，可将表示值域限制在 $\sigma$ 以下；上述转移不改变任何标签，后续反射也保持这个范围。

令 $\rho(A)$ 为可用稳定表示的最小严格上界。由于上述表示存在，该最小值有定义。对任意非空 $A$，论文定理 2.7 中的局部反射构造，只使用已有稳定表示、引理 2.5 和引理 2.6，即给出
$$\rho(B_n(A))<\rho(A)\qquad(n\in\mathbb N).$$
具体地，若 $\beta$ 是原末列标签，复制块可以重新标记到 $\beta$ 以下，所以新矩阵的表示上界至多为 $\beta<\rho(A)$；无父项分支直接删尾，也得到严格下降。

每个非平凡展开都使序数 $\rho$ 严格下降，因此不存在无限展开链；沿有限非空展开路径仍严格下降，所以其传递闭包也良基。$\square$

这个推广在所给 Lean 源码的接口中也有对应依据：`RepresentationDescentSystem.expand_bounded` 的参数是任意 `ValidArray`，没有 `Generated` 前提；标准生成性用于原来的初始表示存在性归纳。本文给出的 $U_{h,m}$ 则补上了任意数组的初始表示。现已在 [AnyArray.lean](formalization/ZeroY/BMS/AnyArray.lean) 中通过 Lean 验证任意矩阵初始表示、展开良基性和任意根后代可比性。[ZeroYConcrete.lean](formalization/Concrete/ZeroYConcrete.lean) 已将上游构造宇宙实际构造的下降系统代入这些核心定理；具体模型和无额外模型参数的最终结论均通过 Lean 编译及公理依赖审计。

**推论 10。** 对任意固定矩阵 $A$，令 $\operatorname{Desc}(A)$ 包含 $A$ 本身及它的一切有限展开后继，则 $\operatorname{Desc}(A)$ 按本文字典序良序。$A$ 无须标准。

**证明。** 定理 9 已给出展开生成关系的良基性，只需证明它在 $\operatorname{Desc}(A)$ 内全序。空起点的闭包只有自身，结论平凡；对非空起点按 $\rho(A)$ 作归纳。任意两个直接展开 $P=B_i(A),Q=B_j(A)$，若 $i\le j$，则 $P$ 是 $Q$ 的前缀。

从 $Q$ 出发的一条有限展开路径，在经过 $P$ 以前，始终保留 $P$ 作为前缀：每次展开都保留原矩阵去末列的前缀，列数最多减少 1，所以第一次缩短到不超过 $|P|$ 时必恰好等于 $P$。因此，对 $C\in\operatorname{Desc}(Q)$，或者 $P$ 是 $C$ 的前缀，或者 $C\in\operatorname{Desc}(P)$。

现在取 $B\in\operatorname{Desc}(P)$。前一种情况下，由连续参数 0 展开可从 $C$ 到达 $P$，再到达 $B$，故二者可比。后一种情况下，$B,C$ 同属 $\operatorname{Desc}(P)$，且 $\rho(P)<\rho(A)$，由归纳假设可比。起点 $A$ 与自身后继当然可比，故全序性成立。

每一步非空展开又严格降低字典序，所以此全序就是 $\operatorname{Desc}(A)$ 上的字典序；它因而良序。$\square$

**对 0-Y 的结论。** 由全域展开交换，任意合法 $s$ 的连续非空 $E_n$ 展开都会终止，其固定起点展开闭包也按字典序良序。Wiki 的 $W_q(s)$ 是 $E_q(s)$ 的前缀，因此其编码也可由 $D(s)$ 经一次 BM4 展开及若干次参数 0 展开得到，故 Wiki 版本的任意起点展开同样终止。

这与第 3.3 节的全域字典序下降链不矛盾。例如 $(0)(1)>(0)(0)(1)$ 是字典序比较，后一矩阵并不是前一矩阵的展开后继：前一矩阵展开后只剩若干零列，此后只能删尾。不同起点的全部可逆矩阵合在一起，仍不构成字典序良序。

## 八、形式化及表示范围

[独立 Lean 工程](formalization/README.md) 已完成全部形式化：核心统一构建通过 66 个任务，43 个关键声明通过公理依赖审计。已验证山脉有限性、父图栈引理、往返、保序、I/S 充要条件及展开保持、两个独立算法的展开交换、两种基本列约定的可达闭包一致，以及抽象降界系统下的全部良基性和终止性定理。

具体构造宇宙模型及所需依赖也已使用 Lean 4.33.1 从锁定源码完成构建，共 1576 个任务通过；模型自身和 13 个无需额外模型参数的最终定理通过另外 14 项公理审计。因此上述良序性和终止性结论不再以调用者另行提供下降系统为前提。全部 57 项关键声明的依赖均为 Lean 标准公理 `propext`、`Classical.choice`、`Quot.sound` 的子集，无 `sorryAx` 或额外自定义公理。实际输出、工具链及两个保持公开陈述不变的上游证明性能补丁见 [验证记录](formalization/VALIDATION.md)。

形式化使用无限精度自然数和任意长度的有限列表。`encodeRaw` 按父链深度编码，`decodeRaw` 按父链反向求和，`expandYRaw` 独立定义复制山脉与向下求和；展开交换是后续定理，不是展开的定义。

共同尾零行不改变父图、解码、展开或可逆性。核心使用去除共同尾零行的规范矩阵；`roundTrip_iff_structural_of_rectangular` 进一步把原始往返等式与 I/S 的充要条件推广到任意矩形表示。零行时仍保留列数，因此 `[]`、`[[]]`、`[[],[]]` 分别表示空矩阵、一个零列和两个零列。

全部合法式与全部可逆矩阵的序同构没有标准性前提。良序结论的范围则是标准生成集，或任意固定起点的后代集；任意合法起点、任意指标序列的实际展开迭代最终到达空式。全体合法式的全域字典序不良基，反例亦已形式化。

## 来源与复核材料

1. 初始实现参考为 BMS 展开器、0-Y 展开器和互转程序。本文最终采用独立定义的数学算法；外部程序不作为证明前提，也未包含在仓库中。
2. [Googology Wiki：0-Y](https://wiki.googology.top/index.php/0-Y)，页面最后编辑 2026-02-25。本文不把其中“等价”的描述当作已证定理；页面也明确注明此前尚无严格证明。
3. Samuel Vargovčík, [Well-Orderedness of the Bashicu Matrix System](https://arxiv.org/abs/2307.04606)。本文读取的 PDF 为 v2；复用定义 1.1、祖先复制引理 2.5 及 BM4 良序结论。
4. [BMS-Well-Ordering-Lean](https://github.com/EgoFakeFantasy/BMS-Well-Ordering-Lean)，本次基线提交 `bae7e3d741f24a56d80da9b99c1345562cd10c2d`，另有两个不改变公开定理陈述、公式定义与假设的本地证明性能补丁。已重建本工程导入的 BMS 核心模块、`ConstructibleBridge.FinalAssembly` 及其依赖，并审计新增关键声明、具体模型和最终定理；准确补丁与锁定源码信息见 [具体模型说明](formalization/Concrete/README.md)。
5. 仓库中的 [Lean 公理审计清单](formalization/Audit.lean) 与 [具体模型公理审计清单](formalization/Concrete/Audit.lean)；实际运行结果见 [验证记录](formalization/VALIDATION.md)。辅助样例见 [Examples.lean](formalization/Examples.lean)，有限样例不承担全称命题的证明。
